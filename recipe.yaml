qs:
  repos:
    - path: .
      name: repo
  templates:
    - docker/install_docker.sh
    - docker/configure_docker_daemon.sh
    - docker/install_docker_compose.sh
    - docker/verify_docker.sh

repo/docker/configure_docker_daemon.sh:
  mirror_auto_yes: "true"
