#!/bin/bash

# @description This helper intentionally has no @qs template marker.
# @step It must not appear in repo inspect, generated .qs/index.json, or search results.
# @shell bash
echo "helper-only"
